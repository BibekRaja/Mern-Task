

/* 

    find if a given value is a number if number positive or negative 
     1
     (-10)
     "John"  //should print => the given data is not a number

     
    - make use of if else 
    - typeof
    
    

*/

let value = 1;

type = typeof (value);
console.log(type);
check = typeof (012)
if (type == check) {
    console.log("The given data is a number");

} else {
    console.log("The given data is not a number");
}